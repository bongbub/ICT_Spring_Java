<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	  
<mapper namespace="spring.mvc.spring_pj_ict05.dao.CustomerDAO">  <!-- namespace="패키지명.인터페이스" -->
	
	<!-- ID 중복확인 처리 -->  
	<!-- 호출할 때 받아온 strId넣어줌(parameterType 주의) -->
	<select id="useridCheck" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM mvc_customer_tbl WHERE user_id = #{strId}
		
	</select>
	
	<!-- 회원가입 처리 -->
	<!-- 멤버변수getter로 꺼내오는 것과 같음 -->
	<insert id="insertCustomer" parameterType="spring.mvc.spring_pj_ict05.dto.CustomerDTO">
		INSERT INTO mvc_customer_tbl (user_id, user_password, user_name, user_birthday, user_address, user_hp, user_email, user_regdate)
					 VALUES(#{user_id}, #{user_password}, #{user_name}, #{user_birthday}, #{user_address}, #{user_hp}, #{user_email}, #{user_regdate})
	</insert>
	
	<!-- 로그인 처리 / 회원정보 인증(수정, 탈퇴) -->
	<select id="idPasswordChk" parameterType="java.util.Map" resultType="int" >
		SELECT COUNT(*) FROM mvc_customer_tbl WHERE user_id = #{strId} AND user_password = #{strPwd}
	</select>
 </mapper> 