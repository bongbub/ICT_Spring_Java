package spring.mvc.basic_ict05.ch11;

import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.stereotype.Component;

@Component("lgtv")	
public class LgTV implements TV{

	
	// expected single matching bean but found 2: [apple, sony]
	// @Autowired			
	// private Speaker speaker;			
	// ---> Speaker 인터페이스 클래스(자식객체 Apple, Sony) 가 두개이므로 안됨 만약 한개였다면 ok
	//  ---> 지금과 같이, 구현클래스가 두 개 이상일 때, 콕 찝어서 지정해주는 어노테이션(@Qualifier) ==> 자격을 부여한다
	
	
	 /*  @Qualifier("sony") : 자격부여 의미
	    * 객체가 여러개 일때(인터페이스 의미) 어떤 객체를 할당할지를 기술하며, 특정객체의 이름을 이용해서 의존성주입한다.
	    * 
	    * Quilifier 생략시 에러메시지 => expected single matching bean but found 2: apple,sony
	    *  Speaker speaker = new SonySpeaker();
	    *  Speaker speaker = new AppleSpeaker();
	    *  
	    */
	
	
	@Qualifier("apple")
	private Speaker speaker;			
								
	// 디폴생
	public LgTV() {
		System.out.println("LgTV의 디폴트생성자");
	}
	
	@Override
	public void volUP() {
		//System.out.println("LgTV - volUP()");
		speaker.volumeUp();
	}

	@Override
	public void volDo() {
		//System.out.println("LgTV - vulDo()");
		speaker.volumeDown();
	}

	@Override
	public void powerOn() {
		System.out.println("LgTV - powerOn()");
	}

	@Override
	public void powerDown() {
		System.out.println("LgTV - powerDown()");
	}
	
}
